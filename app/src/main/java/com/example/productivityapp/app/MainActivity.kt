package com.example.productivityapp.app

/*
 Legacy/duplicated MainActivity from the FocusFlow import. Renamed to avoid conflicts with
 the app's real MainActivity in package com.example.productivityapp.

 The original placeholder class was removed to avoid unused-class warnings. The file is kept
 as a comment so the imported source can be referenced if needed.
 */
