package com.example.productivityapp.app.data.repository

import android.content.Context

/**
 * WaterRepository has been removed in favor of using `UserDataStore` directly.
 * This shim remains as a deprecated placeholder and will be deleted in a follow-up cleanup.
 */
@Deprecated("Use UserDataStore directly; WaterRepository removed")
class WaterRepository(context: Context) {
    init {
        // Intentionally do nothing. Construction of this class is deprecated.
    }

}
